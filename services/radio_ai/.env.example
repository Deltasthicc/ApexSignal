RADIO_AI_PORT=8002
HF_TOKEN=your_hugging_face_token_here
HF_ASR_MODEL=openai/whisper-small
HF_TONE_MODEL=audeering/wav2vec2-large-robust-12-ft-emotion-msp-dim

# fixture | live. fixture returns contracts/fixtures/radio_analysis_output.sample.json
# unchanged, so downstream services and the UI can be built before models are wired up.
ANALYZE_MODE=fixture
