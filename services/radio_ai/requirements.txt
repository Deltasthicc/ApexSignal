# Web service
fastapi>=0.115
pydantic>=2.7,<3.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# ASR: faster-whisper (CTranslate2 runtime), loads distil-whisper/distil-large-v3.5-ct2
faster-whisper>=1.0
nvidia-cublas-cu12  # ctranslate2's GPU path needs libcublas.so.12 even on a CUDA-13 torch install

# Acoustic tone: VoiceCLAP encoder + attribute heads (transformers + torchaudio)
transformers>=4.44
torch>=2.3
torchaudio>=2.3
torchcodec>=0.10  # torchaudio.load() delegates to this since torchaudio 2.9+; missing it fails at decode time, not import time
soundfile>=0.12
numpy>=1.24  # imported directly in asr.py/audio_preprocessing.py/pipeline.py/tone.py -- pin it, don't rely on a transitive torch/transformers version

# Complaint classifier: embedding-prototype backend (default as of
# 2026-08-14, see VALIDATION_GATES.md gate 6d), plus NLI (DeBERTa v3
# zero-shot, sentencepiece tokenizer) kept selectable via
# CLASSIFIER_BACKEND. Version constraint matches services/core_api's
# own sentence-transformers pin for consistency, not a fresh choice.
sentence-transformers>=3.0
sentencepiece>=0.2

huggingface_hub>=0.24

# Day-1 candidate-shortlist script only (services/radio_ai/scripts/shortlist_candidate_clips.py)
pyarrow>=16.0
fsspec>=2024.6

pytest>=8.3
httpx>=0.27
